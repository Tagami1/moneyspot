# Spec（仕様書）

## 要件
### 機能要件（やること）
- OpenStreetMapから世界中の両替ショップ（amenity=bureau_de_change）を取得
- 店舗データをSupabaseに保存（位置、名前、住所、データソース）
- Travelexの各国サイトからレートをスクレイピング
- 参考レート（中間レートAPI）をレートがない店舗のフォールバックとして表示
- 「実店舗レート」と「参考レート」をUIで区別して表示
- Cloudflare Workersで定期実行（OSM取得: 週1回、レート: 1日3回）

### 非機能要件（品質・性能など）
- PostGIS空間インデックスで店舗数が増えてもクエリ速度を維持
- OSMデータ取得時にAPIレート制限を守る（Overpass API利用ポリシー準拠）
- 既存の日本の店舗データとの共存（データソースで区別）

## 制約条件
- OSM Overpass APIは無料だがレート制限あり（1リクエスト/10秒程度）
- Travelexのサイト構造は国ごとに異なる可能性あり
- 参考レートAPIは無料枠のあるものを使用（open.er-api.com等）

## 完成条件（これを満たせばOK）
- [ ] 東京・ロンドン・バンコク・パリで20km圏内のショップが表示される
- [ ] Travelex店舗で実店舗レートが表示される
- [ ] レートがない店舗には参考レートが表示される
- [ ] UIで「実レート」と「参考レート」の違いが分かる
- [ ] 定期実行が動作する

## スコープ外（今回やらないこと）
- ユーザーによるレート投稿機能
- 全チェーンの個別パーサー作成
- ATM・銀行の対応
- オフライン対応
