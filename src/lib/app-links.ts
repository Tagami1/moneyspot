/**
 * Native app store links. iOS is live ("MoneySpot FX", id 6761978909).
 * Android is not public yet (internal/draft) — leave null until released,
 * then set ANDROID_APP_URL and badges appear automatically.
 */
export const IOS_APP_URL = "https://apps.apple.com/app/moneyspot-fx/id6761978909";
export const ANDROID_APP_URL: string | null = null; // set when Play listing is public
